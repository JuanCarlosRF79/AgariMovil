<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AgariMovil" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/primero</item>
        <item name="colorPrimaryDark">@color/primero</item>
        <item name="colorAccent">@color/white</item>
        
        <item name="android:fontFamily">@font/nunito_bold</item>
        
        <item name="android:statusBarColor">@color/primero</item>
        <item name="android:navigationBarColor">@color/primero</item>
    </style>
    <style name="Theme.AgariMovil" parent="Base.Theme.AgariMovil"/>

    <style name="Theme.AgariMovil.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AgariMovil.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">

    </style>

    <style name="Theme.AgariMovil.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" >
        <item name="colorPrimary">@color/primero</item>
        <item name="colorPrimaryDark">@color/primero</item>
        <item name="colorAccent">@color/white</item>
    </style>
</resources>